int intervalo = 2000;
int led = 3;

void setup (){
    pinMode(led, OUTPUT);
}
void loop (){
    for (int i=0; i<10; i++){
      digitalWrite(led, HIGH);
      delay(intervalo);
      digitalWrite(led, LOW);
      delay(intervalo);
      intervalo = intervalo - 100;  
    }
}
